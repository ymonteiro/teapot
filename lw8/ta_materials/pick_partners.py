def pick_partners(L: list, group_size: int) -> zip:
    '''
    Pre: A list of utorids
    Post: A zip object containing pairs or trios of utorids
    '''
    
    # group_size divides list evenly
    if (len(L) % group_size) == 0:
        cut = len(L) // group_size
        grand_lst = []
        for l in range(group_size):
            grand_lst.append()
        return zip(L1, L2)
